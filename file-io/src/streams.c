#include <stdio.h>

int main(void) {

  // printf is a function that writes to the standard output stream (stdout)
  printf("Hello, world!\n");
  fprintf(stdout, "Hello, world!\n");  // printf to a file

  return 0;
}